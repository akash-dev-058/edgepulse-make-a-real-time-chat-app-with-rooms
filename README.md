# A real-time multi-room chat application with user accounts, message history, and moderation tools

> Generated by EdgePulse AI — Production-ready, fully-implemented application.

## Overview
A real-time multi-room chat application with user accounts, message history, and moderation tools is a Next.js (React) + Socket.io + PostgreSQL + Redis + Tailwind CSS application built with best practices for security, performance, and scalability.

## Tech Stack
Next.js (React) + Socket.io + PostgreSQL + Redis + Tailwind CSS

## Architecture
```
.
├── backend/              # API server & business logic
│   ├── app/
│   │   ├── models/       # Database models (SQLAlchemy / Prisma)
│   │   ├── routes/       # API route handlers
│   │   ├── services/     # Business logic layer
│   │   ├── schemas/      # Request/response validation
│   │   ├── middleware/   # Auth, logging, rate-limit
│   │   └── utils/        # Shared utilities
│   └── tests/            # Unit & integration tests
├── frontend/             # UI application
│   └── src/
│       ├── components/   # Reusable UI components
│       ├── pages/        # Route-level page components
│       ├── hooks/        # Custom React hooks
│       ├── api/          # API client functions
│       └── store/        # State management
├── docker-compose.yml    # Dev + prod container config
├── .env.example          # Required environment variables
└── Makefile              # Common dev commands
```

## Quick Start

### Prerequisites
- Node.js 20+ or Python 3.12+
- Docker & Docker Compose (recommended)
- Git

### Using Docker (Recommended)
```bash
git clone <repo>
cd <repo>
cp .env.example .env
# Edit .env with your configuration values
docker-compose up --build
```
Application available at: http://localhost:3000

### Manual Setup
```bash
# Backend
cd backend
python -m venv .venv && source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r requirements.txt
cp ../.env.example ../.env && nano ../.env           # fill in your values
alembic upgrade head                                  # run migrations
python main.py

# Frontend (separate terminal)
cd frontend
npm install
npm run dev
```

## Environment Variables
Copy `.env.example` to `.env` and fill in all values before running.

| Variable | Description | Required |
|----------|-------------|----------|
| `DATABASE_URL` | PostgreSQL connection string | ✅ |
| `SECRET_KEY` | JWT signing secret (min 32 chars) | ✅ |
| `CORS_ORIGIN` | Frontend URL for CORS | ✅ |
| *(see .env.example for full list)* | | |

⚠️ **Never commit `.env` to version control.**

## Available Commands
```bash
make dev          # Start dev environment
make test         # Run all tests
make lint         # Run linter + type-check
make migrate      # Apply database migrations
make build        # Build production images
make deploy       # Deploy to production
make logs         # Tail container logs
```

## Build Plan
  1. Step 1: Set up user auth and room models in database
  2. Step 2: Implement WebSocket server with Socket.io for real-time messaging
  3. Step 3: Build room creation, joining, and UI flows
  4. Step 4: Add message history, moderation, and UI polish
  5. Step 5: Deploy and test with real users

## API Reference
API documentation available at `http://localhost:8000/docs` (Swagger UI) once the backend is running.

## Deployment
1. Set all environment variables in your hosting provider's secrets manager
2. Run database migrations: `make migrate`
3. Build and push Docker images: `make build`
4. Configure HTTPS (update `nginx.conf` with your SSL certificate paths)
5. Set `NODE_ENV=production` / `ENVIRONMENT=production`

## Contributing
1. Fork the repository
2. Create a feature branch: `git checkout -b feature/your-feature`
3. Commit changes: `git commit -m 'feat: add your feature'`
4. Push and open a Pull Request

---
*Built with [EdgePulse AI](https://edgepulse.ai)*
